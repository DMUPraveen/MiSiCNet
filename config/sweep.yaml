defaults:
  - override hydra/sweeper: optuna
  - override hydra/sweeper/sampler: tpe


hydra:
  sweep:
    dir: outputs/multirun/${dataset}/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  sweeper:
    sampler:
      seed: 123
    direction: minimize
    study_name: sphere
    storage: null
    n_trials: 20
    n_jobs: 1
    params:
      lamb: tag(log, interval(0.1,100))




dataset: ??? 
lr: 0.001
epoch: 8000
lamb: 100
